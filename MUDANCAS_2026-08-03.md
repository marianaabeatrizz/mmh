# Mudanças em `pipeline_completo_mmh.py` — 2026-08-03

Todas as alterações foram feitas **no próprio arquivo** `pipeline_completo_mmh.py`
(edição in place; não há cópia separada). Objetivo: alinhar o pipeline ao PDF de
arquitetura (`rede_semantica_catmat.txt`) inserindo o **LLM (gpt-4o)** nas fases
corretas e trocando o encoder da fase [5].

---

## 1. Fase [8b] — migração Anthropic → OpenAI
- Antes: `anthropic` + `claude-haiku-4-5-20251001`.
- Agora: `openai.OpenAI` + `client.chat.completions.create`, `LLM_MODEL = "gpt-4o"`.
- Lê a chave em `os.environ.get("OPENAI_API_KEY")` — **linha ~1927**.

## 2. Fase [2] — Extração agora é LLM (camada "Percepção" do PDF)
- Antes: 100% regex.
- Agora: `extrair_atributos_pdm()` (**linha ~564**) usa gpt-4o em modo JSON,
  aplicado aos **dois lados** (e-Fisco + CATMAT), com:
  - `_construir_cliente_openai()` — **linha ~443** (lê `OPENAI_API_KEY`, **linha ~449**)
  - `_canonicalizar_atributos_llm()` — força o mesmo formato do regex
  - `extrair_atributos_efisco_llm()` — chamada + cache
  - **Fallback regex** quando não há chave/SDK ou em erro de API
  - **Cache em disco:** `cache_extracao_llm.json` (`_CACHE_EXTRACAO_PATH`, **linha ~437**)
- `fase_2_extracao` reescrita; novos stats: `extracao_llm`, `extracao_regex`, `extracao_pct_llm`.

## 3. Fase [5] — bi-encoder ASSIMÉTRICO E5
- Antes: `paraphrase-multilingual-MiniLM-L12-v2` (simétrico).
- Agora: `MODELO_EMBEDDING = "intfloat/multilingual-e5-base"` — **linha 185**.
- Prefixos assimétricos na codificação: `query:` (e-Fisco) / `passage:` (CATMAT),
  via flag `_USA_PREFIXO_E5`.
- Stats: `modelo_embedding`, `encoder_assimetrico`.

## 4. Fase [6] — GraphRAG com adjudicação por LLM
- Antes: só votação de vizinhança (sem LLM).
- Agora: `_adjudicar_graphrag_llm()` — **linha ~1347** — o gpt-4o julga o par
  com o **contexto da vizinhança** recuperada no grafo (PDF §4.2).
  - Devolve `{score, justificativa}`; nova coluna `graphrag_justificativa` (também no YAML).
  - **Fallback:** votação estatística (`0.6×neural + 0.4×vizinhos`).
  - Teto `LLM_MAX_ADJUDICACOES = 500` — **linha ~1344** + cache `cache_graphrag_llm.json`.
- Stats: `adjudicacao_llm`, `adjudicacao_votacao`.

## 5. Outros
- Imports adicionados no topo: `os`, `hashlib`.
- Docstring do módulo (fases [2], [5], [6]) atualizada.
- Arquivo vazio `poo.py` removido.

---

## Onde a API key é lida
Só em `pipeline_completo_mmh.py`, via **variável de ambiente `OPENAI_API_KEY`**,
nas linhas **~449** (fases [2]/[6]) e **~1927** (fase [8b]). Não há arquivo de chave.
