# Pipeline neuro-simbólico CATMAT <-> e-Fisco (MMH)
# Versões fixadas no ambiente em que o pipeline foi validado (Python 3.13).
#
#   pip install -r requirements.txt
#
# O reasoner OWL (Pellet, embarcado no owlready2) precisa de Java no PATH.
# Verifique com: java -version

# --- Dados e numérico ---
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.9.0
PyYAML==6.0.3

# --- Grafos ---
networkx==3.6.1          # grafo léxico, similaridade, comunidades (Louvain)

# --- Camada simbólica ---
owlready2==0.51          # ontologia OWL + SWRL + reasoner Pellet (requer Java)
rdflib==7.6.0            # vista SKOS/RDF do léxico (§3.5) + grafo de dados SHACL (fase [2])
pyshacl==0.40.1          # validação de esquema PDM (fase [2], Figura 1)

# --- PLN e matching ---
sentence-transformers==5.5.1   # bi-encoder E5 + cross-encoder mMARCO
torch==2.12.0                  # backend dos modelos (CPU basta)
gensim==4.4.0                  # indução de sinônimos por fastText (§3.6-d)
rapidfuzz==3.14.5              # similaridade de string (fallback do estágio 2)

# --- Camada de percepção (LLM) ---
openai==2.41.1           # extração de atributos e adjudicação GraphRAG
graphrag==3.1.2          # Microsoft GraphRAG (fase [6]: indexação + local search).
                         # Instala litellm, lancedb, spacy e ~60 dependências; o
                         # workspace por dataset é criado pelo próprio pipeline
                         # (catalogo_match.graphrag.garantir_workspace).

# --- Serviço (§3.8) ---
fastapi==0.141.1
uvicorn==0.52.3
pydantic>=2.0

# --- Visualização ---
matplotlib==3.11.0
